namespace Rules{
    export const REQUIRED = 'required';
    export const ALPHA_ONLY = 'alpha-only';
    export const MAX_LENGTH = 'max-length';
    export const MIN_LENGTH = 'min-length';
    export const POSTAL_CODE = 'postal-code';
}

export default Rules;